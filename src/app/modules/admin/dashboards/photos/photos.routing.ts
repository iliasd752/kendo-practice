import {Route} from '@angular/router';
import {PhotoSlideComponent} from './photo-slide/photo-slide.component';



export const photoRoutes: Route[] = [
    {
        path     : '',
        component: PhotoSlideComponent
    }
];
